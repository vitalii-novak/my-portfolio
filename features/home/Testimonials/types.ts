export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initials: string;
  from: string;  // gradient start colour
  to: string;    // gradient end colour
  ring: string;  // avatar ring rgba colour
}
